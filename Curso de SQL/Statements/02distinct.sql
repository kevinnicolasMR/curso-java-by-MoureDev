/*Cuales son los campos diferentes*/
SELECT distinct * FROM users;

/*Basicamente permite diferenciar datos que son diferntes, y los datos que se repita los ignora*/
SELECT distinct age_user FROM users;

